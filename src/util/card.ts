import { BaseCommand, BaseSession, Card, CommandFunction } from "kasumi.js";
import type RawChannelListResponse from "kasumi.js";

const createCard = (data: RawChannelListResponse) => {
    var card: Card;
}